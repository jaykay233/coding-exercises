对于一个有向图，连通分量：对于分量中任意两点u,v,必然可以从u走到v,且从v走到u
强连通分量,极大联通分量,通过缩点转换成拓补图
树枝边(x,y)
前向边(x,y) x是y祖先
后向边(x,y) y是x祖先
横插边(x,y) 往树的左子树方向的边

连通分量编号递减的顺序一定是拓补序

如何求割点
low(y)>=dfn(x)
如果x不是根结点，那么x是割点
如果x是根结点，那没有至少要有两个子节点y_i并且满足low(y_i)>=dfn(x)
